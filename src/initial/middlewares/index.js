import { default as auth } from './auth'
import { default as guest } from './guest'
import { default as resetPassword } from './resetPassword'
import { default as fetchProps } from './fetchProps'
import { default as userRequest } from './userRequest'

export default {
  auth,
  guest,
  resetPassword,
  fetchProps,
  userRequest,
}
